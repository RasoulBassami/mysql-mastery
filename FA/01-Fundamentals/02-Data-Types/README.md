<div dir="rtl">

# بخش ۱، درس ۲: انواع داده (Data Types) و انتخاب بهینه

انتخاب نوع داده صحیح برای ستون‌های جدول، یکی از پایه‌ای‌ترین و در عین حال تأثیرگذارترین مهارت‌ها در طراحی دیتابیس است. این انتخاب مانند انتخاب یک جعبه با اندازه‌ی مناسب برای یک وسیله است؛ یک جعبه‌ی زیادی بزرگ، فضا را هدر می‌دهد و یک جعبه‌ی کوچک، وسیله را خراب می‌کند.

انتخاب درست Data Type دو هدف اصلی را دنبال می‌کند:

1.  **یکپارچگی داده (Data Integrity):** دیتابیس اجازه نمی‌دهد داده‌ی نامعتبر در ستون ذخیره شود. مثلاً شما نمی‌توانید یک متن را در ستونی از نوع عددی ذخیره کنید.
2.  **بهینه‌سازی فضا و عملکرد (Space & Performance):** استفاده از کوچکترین نوع داده‌ی ممکن، حجم جدول را کاهش می‌دهد. جداول کوچکتر به معنای استفاده کمتر از دیسک، انتقال سریع‌تر داده و جا شدن حجم بیشتری از اطلاعات در حافظه RAM است که همگی به **افزایش سرعت** منجر می‌شوند.

-----

## 🔢 انواع داده عددی (Numeric Types)

برای ذخیره اعداد، خانواده `INTEGER` گزینه‌های مختلفی با اندازه‌های متفاوت در اختیار ما می‌گذارد.

|  نوع داده   | حجم ذخیره‌سازی   | محدوده (حالت Signed)              | محدوده (حالت Unsigned)   |
|:------------|:----------------:|:----------------------------------|:-------------------------|
| `TINYINT`   |      1 بایت      | 128-  تا 127                      | 0 تا 255                 |
| `SMALLINT`  |      2 بایت      | 32,768-  تا 32,767                | 0 تا 65,535              |
| `MEDIUMINT` |      3 بایت      | 8,388,608-  تا 8,388,607          | 0 تا 16,777,215          |
| `INT`       |      4 بایت      | حدود 2.1-  میلیارد تا 2.1 میلیارد | حدود 0 تا 4.2 میلیارد    |
| `BIGINT`    |      8 بایت      | بسیار بزرگ\! (حدود 9 کوینتیلیون)  | حدود 0 تا 18 کوینتیلیون  |

**قانون طلایی برای اعداد:**

> همیشه **کوچکترین** نوع داده‌ای را انتخاب کنید که بتواند با اطمینان **بزرگترین مقدار ممکن** برای آن ستون را در خود جای دهد.

* **مثال ۱:** برای ستون `age` (سن کاربر)، `TINYINT UNSIGNED` عالی است، چون سن هیچوقت منفی نیست و از ۲۵۵ هم بیشتر نمی‌شود.
* **مثال ۲:** برای ستون `status` که مقادیر آن بین ۰ تا ۵ است، `TINYINT UNSIGNED` بهترین انتخاب است. استفاده از `INT` در اینجا هدر دادن خالص فضا است.
* **مثال ۳:** برای `id` کاربران، `INT UNSIGNED` (با ظرفیت ۴.۳ میلیارد) برای اکثر سایت‌ها کافی به نظر می‌رسد. اما استفاده از `BIGINT UNSIGNED` از همان ابتدا، یک **استاندارد حرفه‌ای** محسوب می‌شود.

**نکته مهم در مورد `INT` در برابر `BIGINT`:**

> انتخاب `BIGINT` برای کلیدهای اصلی (Primary Keys) یک «بیمه عمر» ارزان در برابر یک ریسک فاجعه‌بار در آینده است. هزینه تغییر `INT` به `BIGINT` در یک جدول بزرگ (با دستور `ALTER TABLE`) می‌تواند باعث روزها از دسترس خارج شدن سیستم شود. هزینه ۴ بایت فضای اضافه `BIGINT` در مقایسه با این ریسک، تقریباً هیچ است. به همین دلیل فریم‌ورک‌های مدرن مانند لاراول به صورت پیش‌فرض از `BIGINT` استفاده می‌کنند.

#### 💡 نکته مهم: انتخاب نوع داده برای واحد پول ایران (ریال/تومان)

هنگام کار با واحدهای پولی که اعشار ندارند و مقادیر آن‌ها می‌تواند بسیار بزرگ باشد (مانند ریال یا تومان)، از نوع `DECIMAL` یا `INT` استفاده نکنید.
حتی `INT UNSIGNED` با سقف حدود ۴.۳ میلیارد، برای مبالغ کلان (مانند ۱۰۰ میلیارد) کافی نیست.
برای ستون‌هایی مانند `amount`، همیشه از **`BIGINT UNSIGNED`** استفاده کنید.

-----

## ✍️ انواع داده متنی (String Types)

دو انتخاب اصلی برای ذخیره متن وجود دارد: `VARCHAR` و `CHAR`.

####  آ) `CHAR(n)`

* **طول ثابت (Fixed-Length):** همیشه به اندازه `n` کاراکتر فضا اشغال می‌کند، حتی اگر متن شما کوتاه‌تر باشد. فضای خالی با space پُر می‌شود.
* **مثال استفاده:** برای داده‌هایی که طول آن‌ها **همیشه ثابت** است، مثل کدهای دو حرفی کشور (`IR`, `DE`)، کدهای وضعیت (`A`, `P`) یا هش‌های MD5.

#### ب) `VARCHAR(n)`

* **طول متغیر (Variable-Length):** فقط به اندازه طول واقعی متن + ۱ یا ۲ بایت برای ذخیره خودِ طول، فضا اشغال می‌کند.
* **مثال استفاده:** تقریباً برای هر نوع متن دیگری با طول متغیر: نام کاربر، ایمیل، عنوان مقاله، آدرس و...

**قانون طلایی برای متن:**

> همیشه از `VARCHAR` استفاده کنید، مگر اینکه ۱۰۰٪ مطمئن باشید که طول داده‌های شما ثابت است.

برای متن‌های بسیار طولانی (مثل متن یک مقاله)، از انواع `TEXT` استفاده کنید (`TINYTEXT`, `TEXT`, `MEDIUMTEXT`, `LONGTEXT`).

-----

## ⏰ انواع داده تاریخ و زمان (Date and Time Types)

دو نوع داده پراستفاده برای ذخیره تاریخ و زمان کامل وجود دارد: `DATETIME` و `TIMESTAMP`.

#### آ) `DATETIME`

* تاریخ و زمان را در فرمت `YYYY-MM-DD HH:MM:SS` ذخیره می‌کند.
* محدوده بسیار بزرگی دارد (از سال 1000 تا 9999).
* **وابسته به Timezone نیست.** مقداری که به آن می‌دهید را عیناً ذخیره و بازیابی می‌کند.
* **کاربرد:** برای ذخیره مقادیر ثابتی که نباید با تغییر Timezone سرور تغییر کنند، مانند **تاریخ تولد**.

#### ب) `TIMESTAMP`

* مانند `DATETIME` عمل می‌کند اما یک تفاوت کلیدی دارد: **وابسته به Timezone است.**
* مقدار ورودی را به **UTC** (زمان هماهنگ جهانی) تبدیل و ذخیره می‌کند. هنگام خواندن، آن را به Timezone فعلی سِشِن MySQL برمی‌گرداند.
* محدوده کمتری دارد (تا سال 2038، که به "مشکل سال ۲۰۳۸" معروف است).
* **کاربرد:** بهترین انتخاب برای ستون‌هایی مثل `created_at` و `updated_at`. این تضمین می‌کند که زمان ثبت یک رویداد برای تمام کاربران در سراسر جهان یکسان دیده شود (با تبدیل به زمان محلی آن‌ها).

**نکته مهم در مورد "مشکل سال ۲۰۳۸":**

> این محدودیت مربوط به سیستم‌های ۳۲ بیتی قدیمی بود. در تمام سرورهای ۶۴ بیتی مدرن و نسخه‌های جدید MySQL (مانند MySQL 8.0 به بعد)، این مشکل **کاملاً حل شده است** و `TIMESTAMP` تا میلیاردها سال آینده به درستی کار خواهد کرد. بنابراین با خیال راحت از آن استفاده کنید.

-----

## 🧩 نوع داده `JSON`

این نوع داده که در نسخه‌های جدید MySQL اضافه شده، به شما اجازه می‌دهد یک ساختار JSON (آبجکت یا آرایه) را مستقیماً در یک ستون ذخیره کنید.

#### چه زمانی خوب است؟ 👍

* برای ذخیره **فراداده‌ها (Metadata)** یا تنظیماتی که ساختار ثابتی ندارند. مثلاً ستون `settings` برای یک کاربر یا ستون `attributes` برای محصولی که ویژگی‌های آن در هر مدل متفاوت است.
* وقتی نمی‌خواهید برای هر ویژگی کوچک یک ستون جدید بسازید.

#### چه زمانی بد است؟ 👎

* **هرگز** برای داده‌های اصلی و رابطه‌ای (Relational) از آن استفاده نکنید.
* اگر نیاز دارید روی یک مقدار به طور مداوم فیلتر (`WHERE`) یا مرتب‌سازی (`ORDER BY`) انجام دهید، آن مقدار باید در ستون مخصوص به خود و با ایندکس مناسب قرار گیرد، نه داخل یک JSON.
* جستجو در JSON به مراتب کندتر از جستجو در یک ستون ایندکس‌شده است.

**قانون طلایی برای JSON:**

> از `JSON` برای انعطاف‌پذیری در داده‌های غیرضروری و متغیر استفاده کنید، نه به عنوان جایگزینی برای طراحی صحیح دیتابیس.

-----

[⏩ درس ۱: موتورهای ذخیره‌سازی](../01-Storage-Engines/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [📄 مشاهده نمونه کد](examples.sql) | [درس ۳: روابط و کلیدها ⏪](../03-Keys-And-Relationships/README.md)

</div>