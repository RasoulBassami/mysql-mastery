<div dir="rtl">

# بخش ۱، درس ۱: آشنایی عمیق با Storage Engines (موتورهای ذخیره‌سازی)

به اولین درس از دوره **mysql-mastery** خوش آمدید! قبل از اینکه وارد جزئیات کوئری‌ها و بهینه‌سازی شویم، باید با بنیادی‌ترین مفهوم در معماری MySQL آشنا شویم: **موتور ذخیره‌سازی (Storage Engine)**.

## موتور ذخیره‌سازی چیست؟

تصور کنید پایگاه داده شما یک انبار بزرگ است. موتور ذخیره‌سازی، **سیستم مدیریت داخلی** این انبار است. این سیستم تعیین می‌کند که داده‌ها چگونه روی دیسک ذخیره شوند، چگونه قفل‌گذاری شوند، چگونه خوانده و نوشته شوند و چگونه از کرش ها بازیابی شوند.

انتخاب موتور ذخیره‌سازی، یکی از مهم‌ترین تصمیمات معماری است که مستقیماً بر **عملکرد، پایداری و قابلیت‌های** دیتابیس شما تأثیر می‌گذارد. در دنیای MySQL، دو بازیگر اصلی و تاریخی وجود دارند: **InnoDB** و **MyISAM**.

---

## انتخاب مدرن و پیش‌فرض (👑 InnoDB)

موتور ذخیره‌سازی **InnoDB**، پیش‌فرض نسخه‌های جدید MySQL است و دلیل خوبی برای این موضوع وجود دارد. این موتور برای برنامه‌های مدرن که به **قابلیت اطمینان بالا** و **همزمانی (Concurrency)** نیاز دارند، طراحی شده است.

**ویژگی‌های کلیدی InnoDB:**

- **پشتیبانی از تراکنش (Transactions):** این مهم‌ترین ویژگی InnoDB است. تراکنش‌ها به شما اجازه می‌دهند چندین دستور SQL را به عنوان یک واحد کاری (all-or-nothing) اجرا کنید. اگر یکی از دستورات شکست بخورد، تمام تغییرات به حالت اول بازمی‌گردد (Rollback). این ویژگی برای حفظ **یکپارچگی داده‌ها** (Data Integrity) در عملیات‌های مالی، ثبت‌نام کاربر یا هر فرآیند چند مرحله‌ای حیاتی است.
- **قفل‌گذاری در سطح سطر (Row-Level Locking):** وقتی یک رکورد در حال ویرایش است، InnoDB فقط همان **یک سطر** را قفل می‌کند. این یعنی کاربران دیگر می‌توانند همزمان سطرهای دیگر همان جدول را ویرایش کنند. این قابلیت برای برنامه‌هایی با تعداد کاربران بالا که همزمان در حال نوشتن اطلاعات هستند (مانند شبکه‌های اجتماعی یا فروشگاه‌های آنلاین) ضروری است.
- **پشتیبانی از کلید خارجی (Foreign Keys):** InnoDB از محدودیت‌های کلید خارجی پشتیبانی می‌کند. این یعنی خودِ دیتابیس، روابط بین جداول را کنترل کرده و اجازه نمی‌دهد داده‌های نامعتبر (مثلاً سفارشی برای یک کاربر ناموجود) ثبت شود.
- **بازیابی خودکار از کرش (Crash Recovery):** InnoDB با استفاده از لاگ‌های تراکنش، در صورت خاموش شدن ناگهانی سرور یا کرش کردن MySQL، می‌تواند به صورت خودکار خودش را بازیابی کرده و داده‌ها را به یک وضعیت پایدار برگرداند.

---

## موتور ذخیره‌سازی قدیمی(📜 MyISAM)

 موتور **MyISAM** پیش‌فرض قدیمی MySQL بود. این موتور ساختار ساده‌تری دارد و در برخی سناریوهای خاص می‌تواند سریع‌تر باشد، اما فاقد بسیاری از ویژگی‌های حیاتی برای برنامه‌های امروزی است.

**ویژگی‌های کلیدی MyISAM:**

-  **عدم پشتیبانی از تراکنش:** بزرگترین نقطه ضعف MyISAM همین است. هیچ تضمینی برای انجام کامل یک عملیات چندمرحله‌ای وجود ندارد.
-  **قفل‌گذاری در سطح جدول (Table-Level Locking):** وقتی یک کاربر در حال نوشتن اطلاعات در یک جدول است، MyISAM **کل جدول** را قفل می‌کند. این یعنی هیچ کاربر دیگری نمی‌تواند حتی یک رکورد دیگر را در آن جدول ویرایش کند تا کار کاربر اول تمام شود. این موضوع به سرعت در ترافیک بالا به یک گلوگاه (Bottleneck) تبدیل می‌شود.
-  **عدم پشتیبانی از کلید خارجی:** MyISAM دستور `FOREIGN KEY` را می‌پذیرد اما آن را **نادیده می‌گیرد** و هیچ محدودیتی را اعمال نمی‌کند.
-  **سرعت بالا در `SELECT`:** به دلیل ساختار ساده‌تر و قفل‌گذاری در سطح جدول، MyISAM معمولاً در عملیات خواندن (به‌خصوص `(*)SELECT COUNT`) کمی سریع‌تر عمل می‌کند.
-  **Full-Text Search (در گذشته):** تا قبل از نسخه 5.6 MySQL، موتور MyISAM تنها گزینه‌ برای جستجوی تمام متن بود. اما امروزه InnoDB هم این قابلیت را به شکل بسیار بهتری ارائه می‌دهد.

---

## جدول مقایسه: InnoDB vs. MyISAM

|  ویژگی (Feature)              |        InnoDB         |      MyISAM                     |
|:------------------------------|:---------------------:|:-------------------------------:|
| **پشتیبانی از تراکنش (ACID)** |       ✅ **بله**       |            ❌ **خیر**            |
| **سطح قفل‌گذاری (Locking)**   | **سطر (Row-Level)**   |     **جدول (Table-Level)**      |
| **کلید خارجی (Foreign Keys)** |       ✅ **بله**       |            ❌ **خیر**            |
| **بازیابی از کرش**            |     **بسیار قوی**     | **ضعیف (نیاز به بازیابی دستی)** |
| **کاربرد اصلی**               |   **عمومی (OLTP)**    |  **خاص و قدیمی (Read-Heavy)**   |

---

## جمع‌بندی: کدام را انتخاب کنیم؟

پاسخ کوتاه و قاطع: **همیشه از InnoDB استفاده کنید.**

> **قانون طلایی:** مگر اینکه دلیل بسیار خاص، مستند و قابل دفاعی برای استفاده از MyISAM داشته باشید، انتخاب شما باید **InnoDB** باشد. قابلیت اطمینان، همزمانی و یکپارچگی داده‌ها که InnoDB ارائه می‌دهد، برای ۹۹.۹٪ برنامه‌های کاربردی امروزی ضروری است.

#### موارد استفاده بسیار نادر از MyISAM:
* **جداول فقط-خواندنی (Read-Only):** جداولی که داده‌های آن‌ها یک بار نوشته شده و میلیون‌ها بار خوانده می‌شوند (مثل جداول کمکی یا لاگ‌ها).
* **سیستم‌های قدیمی:** کار روی پروژه‌های قدیمی که از ابتدا با MyISAM ساخته شده‌اند.

---

### کدهای کاربردی
<div dir="ltr">

دستورات مربوط به؛ مشاهده موتور ذخیره‌سازی یک جدول، ساخت یک جدول جدید با موتور InnoDB، تبدیل یک جدول موجود به InnoDB

```sql
SHOW TABLE STATUS LIKE 'users';


CREATE TABLE products (
    id INT PRIMARY KEY,
    name VARCHAR(255)
) ENGINE=InnoDB;


ALTER TABLE my_legacy_table ENGINE=InnoDB;
```
</div>

---

[🔼 بازگشت به فهرست فصل](../README.md) | [درس ۲: انواع داده ⏪](../02-Data-Types/README.md)

</div>