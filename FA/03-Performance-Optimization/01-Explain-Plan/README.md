<div dir="rtl">

# بخش ۳، درس ۱: تحلیل کوئری‌های کند با `EXPLAIN`

هر زمان که احساس می‌کنید یک کوئری کندتر از حد انتظار اجرا می‌شود، اولین و بهترین دوست شما دستور `EXPLAIN` است.

دستور `EXPLAIN` کوئری شما را **اجرا نمی‌کند**، بلکه **نقشه اجرای (Execution Plan)** آن را به شما نشان می‌دهد. این نقشه، استراتژی گام‌به‌گام MySQL برای پیدا کردن و برگرداندن داده‌های مورد نظر شماست. در واقع `EXPLAIN` مانند یک دستگاه X-ray است که به شما اجازه می‌دهد داخل فرآیند اجرای کوئری را ببینید و نقاط ضعف آن را شناسایی کنید.

### نحوه استفاده

استفاده از آن بسیار ساده است. کافی است کلمه `EXPLAIN` را قبل از کوئری `SELECT` خود قرار دهید:

<div dir="ltr">

```sql
EXPLAIN SELECT * FROM users WHERE id = 1;
```
</div>

خروجی این دستور یک جدول با ستون‌های مختلف است. ما روی مهم‌ترین ستون‌ها تمرکز خواهیم کرد که ۸۰٪ اطلاعات مورد نیاز شما را فراهم می‌کنند.

---

## رمزگشایی خروجی `EXPLAIN`

بیایید ستون‌های کلیدی خروجی را بررسی کنیم.

### ۱. ستون `type` (مهم‌ترین ستون)

ین ستون روش دسترسی MySQL به داده‌ها را نشان می‌دهد و به نوعی، نمره کوئری شماست. این مقادیر از بهترین (سریع‌ترین) به بدترین (کندترین) مرتب شده‌اند:
* آ) `const`: **عالی!** بهترین حالت ممکن. MySQL فقط یک ردیف را با استفاده از `PRIMARY KEY` یا `UNIQUE KEY` پیدا کرده و می‌خواند.
* ب) `eq_ref`: **فوق‌العاده.** معمولاً در `JOIN` ها دیده می‌شود و به این معناست که برای هر ردیف از جدول اول، دقیقاً یک ردیف از جدول دوم با استفاده از ایندکس کلیدی خوانده می‌شود.
* پ) `ref`: **خیلی خوب.** MySQL برای پیدا کردن ردیف(ها) از یک ایندکس غیریکتا (non-unique index) استفاده کرده است.
* ت) `range`: **خوب.** MySQL ردیف‌ها را در یک محدوده مشخص با استفاده از یک ایندکس پیدا کرده است (مثلاً `WHERE id > 100`).
* ث) `index`: **متوسط.** MySQL کل ایندکس را اسکن کرده است. این بهتر از اسکن کل جدول است، اما هنوز ایده‌آل نیست.
* ج) `ALL`: **فاجعه! 🚨** این یعنی MySQL هیچ ایندکس مناسبی پیدا نکرده و مجبور است **کل جدول (Full Table Scan)** را ردیف به ردیف اسکن کند. این اصلی‌ترین چیزی است که باید در کوئری‌های خود از آن اجتناب کنید.

### ۲. ستون‌های `possible_keys` و `key`

* آ) `possible_keys`: لیستی از ایندکس‌هایی که MySQL **فکر می‌کند** می‌تواند از آن‌ها برای این کوئری استفاده کند.
* ب) `key`: ایندکسی که MySQL **در عمل انتخاب کرده** و از آن استفاده می‌کند.

**علامت خطر:** اگر `possible_keys` یک ایندکس را نشان دهد اما `key` برابر `NULL` باشد، یعنی یک مشکلی وجود دارد (مثلاً استفاده از یک تابع روی ستون ایندکس شده در `WHERE`) که مانع استفاده از ایندکس شده است.

### ۳. ستون `rows`

این ستون **تخمین** MySQL از تعداد ردیف‌هایی است که باید بررسی کند تا به نتیجه برسد. هدف شما همیشه باید **کم نگه داشتن این عدد** باشد. یک عدد بزرگ در این ستون، به خصوص وقتی `type` برابر `ALL` باشد، نشان‌دهنده یک کوئری بسیار ناکارآمد است.

### ۴. ستون `Extra`

این ستون حاوی اطلاعات اضافی بسیار ارزشمندی است.

* آ) `Using index`: **عالی!** این یعنی یک **ایندکس پوششی (Covering Index)** استفاده شده و کوئری بدون مراجعه به جدول اصلی، فقط از روی ایندکس پاسخ داده شده است.
* ب) `Using where`: نرمال است و یعنی از شرط `WHERE` برای فیلتر کردن ردیف‌ها استفاده شده.
* پ) `Using temporary`: **بد.** MySQL برای پردازش کوئری (معمولاً در `GROUP BY` یا `ORDER BY` های پیچیده) مجبور به ساخت یک جدول موقت شده است. این عملیات می‌تواند کند باشد.
* ت) `Using filesort`: **بد.** MySQL نتوانسته از یک ایندکس برای مرتب‌سازی (`ORDER BY`) استفاده کند و مجبور شده ردیف‌ها را در یک مرحله جداگانه مرتب کند. این یکی از دلایل اصلی کندی در صفحات با `pagination` است.

---
[⏩ فصل قبلی: طراحی پیشرفته](../../02-Advanced-Design/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [📄 مشاهده نمونه کد](examples.sql) | [درس بعدی: بهینه‌سازی ایندکس ⏪](../02-Index-Level-Optimization/README.md)

</div>