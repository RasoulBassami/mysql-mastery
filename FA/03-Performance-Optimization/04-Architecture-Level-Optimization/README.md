<div dir="rtl">

# بخش ۳، درس ۴: بهینه‌سازی در سطح معماری (Architecture-Level Optimization)

بهینه‌سازی فقط به `EXPLAIN` و ایندکس خلاصه نمی‌شود. یک مهندس ارشد باید دیدی کلان‌نگر داشته باشد و بداند که برخی از مهم‌ترین تصمیمات برای افزایش عملکرد، خارج از خود دیتابیس گرفته می‌شوند.

در این درس، دو استراتژی معماری کلیدی را بررسی می‌کنیم: **کش کردن (Caching)** و **تأثیر انتخاب Data Type در مقیاس بزرگ**.

---

## ۱. استراتژی کش (Caching): سریع‌ترین کوئری، کوئری اجرا نشده است!

مهم نیست چقدر کوئری‌های خود را بهینه می‌کنید، دسترسی به داده از حافظه RAM **همیشه هزاران بار سریع‌تر** از دسترسی به آن از دیتابیس (که شامل I/O دیسک و پردازش است) خواهد بود.

**کش کردن (Caching)** یعنی ذخیره کردن موقت نتایج کوئری‌های پراستفاده یا داده‌های سنگین در یک حافظه سریع (مانند RAM) تا در درخواست‌های بعدی، به جای مراجعه به دیتابیس، داده مستقیماً از کش خوانده شود.

### چه زمانی و چه چیزی را کش کنیم؟

* **داده‌هایی که به ندرت تغییر می‌کنند:** لیست دسته‌بندی محصولات، تنظیمات سایت، پروفایل کاربری که به ندرت آپدیت می‌شود.
* **نتایج کوئری‌های سنگین و پرتکرار:** یک کوئری گزارش‌گیری پیچیده در داشبورد ادمین، لیست پرفروش‌ترین محصولات در صفحه اصلی.
* **داده‌های محاسباتی:** تعداد دنبال‌کنندگان یک کاربر، امتیاز یک محصول. محاسبه این موارد در هر بار درخواست می‌تواند هزینه‌بر باشد.

### ابزارهای رایج برای Caching

ابزارهای کش معمولاً سیستم‌های ذخیره‌سازی **In-Memory** (درون حافظه) هستند که سرعت فوق‌العاده بالایی دارند. دو مورد از محبوب‌ترین‌ها عبارتند از:

* آ) **Redis:** یک ابزار همه‌کاره که ساختارهای داده متنوعی (رشته، لیست، هش و...) را پشتیبانی می‌کند و برای انواع کشینگ، صف‌ها (Queues) و Pub/Sub استفاده می‌شود.
* ب) **Memcached:** یک سیستم کش ساده‌تر و قدیمی‌تر که فقط به صورت Key-Value کار می‌کند و تمرکز آن صرفاً بر روی سرعت است.


**چالش اصلی در کشینگ: بی‌اعتبار کردن کش (Cache Invalidation)**
سخت‌ترین بخش کار با کش این است که بدانید چه زمانی باید داده‌های کش شده را پاک کنید تا کاربر داده‌های قدیمی را نبیند. استراتژی‌های رایج عبارتند از:
* آ) **Time-To-Live (TTL):** برای کش یک زمان انقضا تعیین می‌کنید (مثلاً ۱۰ دقیقه).
* ب) **Cache Busting:** پس از هر بار آپدیت داده در دیتابیس، به صورت دستی نسخه کش شده آن را نیز پاک می‌کنید.

---

## ۲. انتخاب کوچکترین Data Type ممکن (در مقیاس بزرگ)

در بخش اول در مورد انتخاب Data Type صحبت کردیم. حالا می‌خواهیم ببینیم این تصمیمات ساده در مقیاس یک جدول با صدها میلیون رکورد، چه تأثیر شگرفی بر عملکرد می‌گذارند.

فرض کنید در حال طراحی جدول `logs` هستید که روزانه میلیون‌ها رکورد در آن ثبت می‌شود. شما برای ستون `level` (که مقادیر آن "INFO", "WARNING", "ERROR" است) دو انتخاب دارید:

**روش اول (ناکافی):** `level VARCHAR(20)`

**روش دوم (بهینه):** `level_id TINYINT UNSIGNED` و یک جدول جداگانه به نام `log_levels` برای نگهداری نام سطوح.

**تحلیل تأثیر بر عملکرد:**

| جنبه            | `VARCHAR(20)`                                                                                                                                     | `TINYINT` + `JOIN`                                                                                                                   |
|:----------------|:--------------------------------------------------------------------------------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------|
| **فضای دیسک**   | برای هر ردیف حدود ۷-۸ بایت. در یک میلیارد ردیف: **~۸ گیگابایت**.                                                                                  | برای هر ردیف ۱ بایت. در یک میلیارد ردیف: **۱ گیگابایت**.                                                                             |
| **حافظه RAM**   | InnoDB برای کار با داده‌ها و ایندکس‌ها، آن‌ها را در Buffer Pool خود در RAM بارگذاری می‌کند. ستون بزرگتر یعنی **فضای بیشتری از RAM اشغال می‌شود**. | ستون کوچکتر یعنی **تعداد ردیف‌های بیشتری** می‌تواند در همان مقدار RAM جا شود. این شانس پیدا شدن داده در حافظه را به شدت بالا می‌برد. |
| **سرعت ایندکس** | ایندکس روی ستون `VARCHAR` بزرگتر و کندتر است.                                                                                                     | ایندکس روی ستون `TINYINT` بسیار کوچک، فشرده و سریع است.                                                                              |
| **سرعت `JOIN`** | نیازی به `JOIN` ندارد.                                                                                                                            | نیاز به یک `JOIN` کوچک و سریع با جدول `log_levels` دارد.                                                                             |

**نتیجه‌گیری:**
با اینکه روش دوم نیاز به یک `JOIN` اضافه دارد، اما صرفه‌جویی عظیم در فضا (دیسک و RAM) و سرعت بالاتر ایندکس‌ها، در مقیاس بزرگ، این هزینه `JOIN` را به طور کامل جبران کرده و به **عملکرد کلی بسیار بهتری** منجر می‌شود.

> **قانون طلایی معماری:** هر بایت در طراحی یک جدول بزرگ، اهمیت دارد. تصمیمات کوچک شما در طراحی، در مقیاس میلیونی، به تفاوت‌های عملکردی غول‌پیکر تبدیل می‌شوند.

---
[⏩ درس قبلی: بهینه‌سازی کوئری](../03-Query-Level-Optimization/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [درس بعدی: بهینه‌سازی سرور ⏪](../05-Server-Level-Optimization/README.md)

</div> 
