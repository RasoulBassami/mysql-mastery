<div dir="rtl">

# بخش ۲، درس ۴: استفاده از View ها (نماها)

تصور کنید یک کوئری بسیار پیچیده با چندین `JOIN` دارید که بارها و بارها در بخش‌های مختلف اپلیکیشن خود از آن استفاده می‌کنید. مثلاً کوئری‌ای که اطلاعات کامل یک محصول را به همراه نام دسته‌بندی، نام برند و تعداد موجودی انبار برمی‌گرداند.

آیا باید این کوئری پیچیده را در تمام بخش‌های کد خود کپی و پیست کنید؟ این کار منجر به تکرار کد و دشواری در نگهداری می‌شود. اگر منطق کوئری تغییر کند، باید تمام نسخه‌های کپی شده را پیدا کرده و اصلاح کنید.

اینجاست که **View** به کمک ما می‌آید.

## نما(View) چیست؟

یک **View**، یک **جدول مجازی (Virtual Table)** است که بر اساس نتیجه یک کوئری `SELECT` ساخته می‌شود.

* **مجازی است:** View خود داده‌ای را ذخیره نمی‌کند. او مانند یک پنجره یا یک میان‌بُر به داده‌های واقعی است که در جداول اصلی ذخیره شده‌اند.
* **یک کوئری ذخیره شده است:** شما می‌توانید View را مانند یک جدول عادی مورد پرس‌وجو قرار دهید (`SELECT * FROM my_view`)، اما در پشت صحنه، MySQL هر بار کوئری اصلی که View با آن تعریف شده را اجرا می‌کند.

---

## چرا از View ها استفاده کنیم؟

استفاده از View ها سه مزیت اصلی دارد:

### ۱. ساده‌سازی پیچیدگی (Simplify Complexity)
شما می‌توانید یک کوئری پیچیده با چندین `JOIN`، توابع تجمعی (`GROUP BY`) و محاسبات را در یک View کپسوله کنید. پس از آن، توسعه‌دهندگان می‌توانند به سادگی با یک `SELECT` ساده از این View، به نتایج پیچیده دسترسی پیدا کنند، بدون اینکه نیاز باشد منطق داخلی آن را بدانند.

### ۲. افزایش امنیت (Enhance Security)
با استفاده از View می‌توانید دسترسی کاربران به داده‌ها را محدود کنید.
* **محدودیت ستون:** می‌توانید یک View بسازید که فقط شامل ستون‌های غیرحساس یک جدول باشد (مثلاً اطلاعات کارمندان بدون ستون حقوق).
* **محدودیت ردیف:** می‌توانید یک View بسازید که فقط شامل ردیف‌های خاصی باشد (مثلاً `WHERE is_active = 1`).
  سپس به جای دسترسی مستقیم به جدول اصلی، به کاربران دسترسی لازم برای خواندن از این View را می‌دهید.

### ۳. ارائه یک لایه انتزاعی (Provide an Abstraction Layer)
یک View می‌تواند به عنوان یک API پایدار برای ساختار دیتابیس شما عمل کند. شما می‌توانید ساختار جداول اصلی را در پشت صحنه تغییر دهید (مثلاً یک ستون را به دو ستون تقسیم کنید)، اما تا زمانی که View همان نتیجه قبلی را برگرداند، اپلیکیشن‌هایی که از آن View استفاده می‌کنند، متوجه هیچ تغییری نخواهند شد و کد آن‌ها نیاز به تغییر ندارد.

---

## کار با View ها در SQL

دستورات کار با View بسیار ساده هستند.

**ایجاد یک View:**
<div dir="ltr">

```sql
CREATE VIEW view_name AS
SELECT column1, column2, ...
FROM table_name
WHERE condition;
```
</div>

**بروزرسانی یک View:**
اگر بخواهید کوئری یک View موجود را تغییر دهید، از `CREATE OR REPLACE VIEW` یا `ALTER VIEW` استفاده می‌کنید.
<div dir="ltr">

```sql
CREATE OR REPLACE VIEW view_name AS
SELECT new_column_list...;
```
</div>

**حذف یک View:**
<div dir="ltr">

```sql
DROP VIEW view_name;
```
</div>

**نکته مهم:** حذف یک View، هیچ تأثیری بر روی داده‌های جداول اصلی ندارد. فقط تعریف آن جدول مجازی پاک می‌شود.

### معایب و ملاحظات
* **عملکرد:** از آنجایی که View یک کوئری ذخیره شده است، عملکرد آن دقیقاً به اندازه عملکرد همان کوئری است. اگر کوئری اصلی کند باشد، `SELECT` از View هم کند خواهد بود. View به خودی خود جادوی بهینه‌سازی انجام نمی‌دهد.
* **Updatable Views:** در شرایط خاص و ساده، می‌توان روی یک View دستورات `UPDATE` یا `INSERT` را اجرا کرد که مستقیماً بر روی جدول اصلی اعمال می‌شود. اما این کار معمولاً پیچیده و مستعد خطا است و توصیه نمی‌شود.

---
## روتین‌های ذخیره شده: رویه‌ها، توابع و تریگرها

روتین‌های ذخیره شده به شما این امکان را می‌دهند که منطق برنامه را به جای اینکه فقط در کد اپلیکیشن خود (مثلاً PHP) نگه دارید، مستقیماً در سرور MySQL ذخیره و اجرا کنید. این کار می‌تواند به افزایش عملکرد، امنیت و قابلیت استفاده مجدد کد کمک کند.

### ۱. رویه‌های ذخیره شده (Stored Procedures)

یک **Stored Procedure** مجموعه‌ای از دستورات SQL است که با یک نام مشخص ذخیره شده و شما می‌توانید آن را هر زمان که نیاز داشتید، فراخوانی (`CALL`) کنید. آن‌ها مانند توابع در زبان‌های برنامه‌نویسی هستند با این تفاوت که **الزاماً مقداری را برنمی‌گردانند**.

**چرا از Stored Procedure استفاده کنیم؟**
* **کاهش ترافیک شبکه:** به جای ارسال چندین کوئری پیچیده از اپلیکیشن به دیتابیس، شما فقط یک دستور `CALL` ارسال می‌کنید و تمام منطق در خود سرور دیتابیس اجرا می‌شود.
* **کپسوله‌سازی منطق (Encapsulation):** می‌توانید منطق پیچیده کسب‌وکار (مثلاً فرآیند ثبت‌نام کاربر که شامل `INSERT` در چند جدول است) را در یک رویه قرار دهید. این کار باعث می‌شود کد تمیزتر و نگهداری آن آسان‌تر شود.
* **امنیت:** می‌توانید به یک کاربر، اجازه اجرای یک رویه را بدهید بدون اینکه به او دسترسی مستقیم به جداول زیرین آن رویه را داده باشید.

<div dir="ltr">

```sql
-- رویه‌ای که یک کاربر جدید اضافه می‌کند
CREATE PROCEDURE sp_add_new_user(IN user_name VARCHAR(100), IN user_email VARCHAR(150))
BEGIN
    INSERT INTO users (name, email, password_hash)
    VALUES (user_name, user_email, 'default_password');
END;

-- فراخوانی رویه
CALL sp_add_new_user('New User', 'new@example.com');
```
</div>

---

### ۲. توابع ذخیره شده (Stored Functions)

یک **Stored Function** بسیار شبیه به رویه است اما با دو تفاوت کلیدی:
1.  **حتماً باید یک مقدار واحد را برگرداند (`RETURN`).**
2.  می‌توان از آن مستقیماً در کوئری‌های SQL استفاده کرد، درست مانند توابع داخلی MySQL مثل `()NOW` یا `()CONCAT`.

**چرا از Stored Function استفاده کنیم؟**
برای ایجاد محاسبات یا فرمت‌بندی‌های قابل استفاده مجدد که می‌خواهید در کوئری‌های `SELECT` خود از آن‌ها بهره ببرید.

<div dir="ltr">

```sql
-- example
-- تابعی که سطح کاربری را بر اساس تعداد سفارشات برمی‌گرداند
CREATE FUNCTION fn_get_customer_level(customer_id INT)
RETURNS VARCHAR(50)
DETERMINISTIC
BEGIN
    DECLARE order_count INT;
    SELECT COUNT(*) INTO order_count FROM orders WHERE user_id = customer_id;

    IF order_count > 10 THEN
        RETURN 'Gold Customer';
    ELSEIF order_count > 5 THEN
        RETURN 'Silver Customer';
    ELSE
        RETURN 'Bronze Customer';
    END IF;
END;

-- استفاده از تابع در یک کوئری
SELECT name, fn_get_customer_level(id) AS customer_level FROM users;
```
</div>

---

### ۳. تریگرها (Triggers)

یک **Trigger** نوع خاصی از رویه است که شما آن را مستقیماً فراخوانی نمی‌کنید، بلکه در پاسخ به یک رویداد خاص در یک جدول، **به صورت خودکار اجرا می‌شود**.

این رویدادها ترکیبی از موارد زیر هستند:
* **زمان:** `BEFORE` (قبل از) یا `AFTER` (بعد از)
* **عملیات:** `INSERT`, `UPDATE`, `DELETE`

**چرا از Trigger استفاده کنیم؟**
* **حسابرسی (Auditing):** ثبت تمام تغییرات در یک جدول لاگ. مثلاً هر بار که حقوق یک کارمند تغییر کرد، یک رکورد در جدول `salary_changes` ثبت شود.
* **اعمال قوانین پیچیده کسب‌وکار:** پیاده‌سازی قوانینی که با محدودیت‌های عادی دیتابیس قابل اجرا نیستند.
* **همگام‌سازی داده‌های Denormalized:** اگر نام کاربری را در جدول `posts` کپی کرده‌اید، یک تریگر `AFTER UPDATE` روی جدول `users` می‌تواند این تغییر را در تمام پست‌های آن کاربر نیز اعمال کند.

**نکته مهم:** از تریگرها با احتیاط استفاده کنید. چون آن‌ها در پس‌زمینه و به صورت "جادویی" اجرا می‌شوند، استفاده بیش از حد از آن‌ها می‌تواند منطق برنامه را پیچیده و اشکال‌زدایی (Debug) را دشوار کند.

<div dir="ltr">

```sql
-- example
-- تریگری که قبل از ثبت کاربر جدید، ایمیل او را به حروف کوچک تبدیل می‌کند
CREATE TRIGGER trg_before_user_insert
BEFORE INSERT ON users
FOR EACH ROW
BEGIN
    SET NEW.email = LOWER(NEW.email);
END;
```
</div>

در این مثال، `NEW` به ردیفی اشاره دارد که قرار است درج شود.

-----

### ⚠️ هشدار استراتژیک: تله‌ی «منطق در دیتابیس»

بعد از دیدن این قابلیت‌های قدرتمند، به خصوص سرعت بالای `Stored Procedure` ها در کاهش رفت و برگشت شبکه، این وسوسه پیش می‌آید که: **«چرا تمام منطق برنامه را در دیتابیس ننویسیم؟»**

این یک تله رایج است که می‌تواند در بلندمدت به پروژه شما آسیب جدی بزند.

1.  **اشکال‌زدایی (Debugging) و تست:** دیباگ کردن یک `LOOP` یا `IF` پیچیده در یک Stored Procedure **کابوس‌وار** است. در حالی که دیباگ کردن کد PHP با بسیار ساده است. تست واحد (Unit Test) از منطق دیتابیس تقریباً غیرممکن است.

2.  **مقیاس‌پذیری (Scalability):** این **مهم‌ترین** دلیل است. شما می‌توانید سرورهای اپلیکیشن (PHP) را به راحتی و ارزانی مقیاس‌بندی کنید (کافیست ۱۰ سرور جدید اضافه کنید). اما مقیاس‌بندی سرور دیتابیس **بسیار سخت و گران** است. با انتقال منطق به دیتابیس، شما بار پردازش (CPU) را از روی سرورهای اپلیکیشن (که مقیاس‌پذیرند) برداشته و روی دیتابیس (که گلوگاه اصلی شماست) می‌اندازید.

3.  **قفل شدن به یک پلتفرم (Vendor Lock-in):** کد PHP شما قابل حمل است، اما Stored Procedure های شما که با سینتکس MySQL نوشته شده‌اند، روی PostgreSQL یا SQL Server کار نخواهند کرد.

**قانون تعادل:**

* ✅ **از View ها استفاده کنید:** برای ساده‌سازی کوئری‌های `SELECT` پیچیده و تکراری (مانند گزارش‌گیری‌ها).
* ✅ **از Stored Procedure ها استفاده کنید:** برای عملیات‌های **داده‌محور** سنگین (مثل آرشیو کردن داده‌های قدیمی) یا تراکنش‌های پیچیده‌ای که باید به صورت اتمی اجرا شوند.
* ❌ **از منطق کسب‌وکار (Business Logic)** در دیتابیس اجتناب کنید. منطق اصلی برنامه (مثل احراز هویت، ارسال ایمیل، پردازش پرداخت) باید همیشه در **لایه اپلیکیشن (PHP)** باقی بماند.

-----

[⏩ درس قبلی: تراکنش‌ها](../03-Transactions-And-ACID/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [📄 مشاهده نمونه کد](examples.sql) | [فصل بعدی: بهینه‌سازی عملکرد ⏪](../../03-Performance-Optimization/README.md)

</div>
