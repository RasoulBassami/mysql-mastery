<div dir="rtl">

# بخش ۵، درس ۴: ابزارهای دیباگ (Debugbar, Telescope)

نوشتن کد بهینه یک چیز است، و اطمینان از اینکه آن کد در عمل بهینه اجرا می‌شود، چیز دیگری است. ابزارهای دیباگ به شما دیدی شفاف از اتفاقاتی که در پس‌زمینه هر درخواست (Request) در اپلیکیشن شما رخ می‌دهد، می‌دهند.

### چرا به ابزارهای دیباگ نیاز داریم؟

* **شناسایی مشکلات عملکردی:** پیدا کردن سریع مشکلاتی مانند N+1 Query، کوئری‌های کند یا مصرف بیش از حد حافظه.
* **شفافیت:** دیدن تمام کوئری‌های SQL اجرا شده، event ها، job ها و... در یک نگاه.
* **بهبود تجربه توسعه:** سرعت بخشیدن به فرآیند اشکال‌زدایی و توسعه.

دو ابزار اصلی و محبوب در اکوسیستم لاراول برای این کار **Laravel Debugbar** و **Laravel Telescope** هستند.

---

## ۱. ابزارهای محیط توسعه (Local)
این ابزارها سربار (Overhead) زیادی دارند و **نباید** روی سرور پروداکشن فعال باشند (مگر با تنظیمات بسیار خاص).

### ## داشبورد شما در لحظه Laravel Debugbar

**ابزار Debugbar** یک پکیج بسیار محبوب است که یک نوار ابزار قدرتمند را در پایین صفحه اپلیکیشن شما (در محیط توسعه) اضافه می‌کند. این نوار ابزار اطلاعات زنده‌ای در مورد درخواست فعلی به شما می‌دهد.

**مهم‌ترین قابلیت‌های آن برای کار با دیتابیس:**

* **تب Queries:** این تب **تمام کوئری‌های دیتابیس** که برای رندر شدن صفحه فعلی اجرا شده‌اند را به همراه **زمان اجرای** هر کدام لیست می‌کند.
* **تشخیص N+1:** اگر در این تب تعداد زیادی کوئری مشابه و تکراری دیدید، این یک زنگ خطر بزرگ برای وجود مشکل N+1 است.
* **نمایش کوئری‌های تکراری:** Debugbar به صورت هوشمند کوئری‌های دقیقاً یکسان را شناسایی و به شما هشدار می‌دهد.

نصب و راه‌اندازی آن بسیار ساده و از طریق Composer انجام می‌شود:

<div dir="ltr">

``` bash 
    composer require barryvdh/laravel-debugbar --dev
```
</div>

---

### ## مرکز فرماندهی شما Laravel Telescope

**ابزار Telescope** ابزار دیباگ رسمی و بسیار قدرتمند لاراول است. Telescope فراتر از یک نوار ابزار ساده است؛ او یک رابط کاربری کامل و زیبا برای نظارت و دیباگ **تمام جنبه‌های اپلیکیشن شما** در محیط لوکال است.

**تفاوت‌ها و برتری‌های Telescope نسبت به Debugbar:**

* **تاریخچه درخواست‌ها:** برخلاف Debugbar که فقط اطلاعات درخواست فعلی را نشان می‌دهد، Telescope تمام درخواست‌ها را **ذخیره می‌کند** و شما می‌توانید به راحتی بین آن‌ها جابجا شده و آن‌ها را تحلیل کنید.
* **رابط کاربری اختصاصی:** Telescope یک داشبورد کامل در آدرس `telescope/` اپلیکیشن شما فراهم می‌کند.
* **نظارت جامع:** علاوه بر کوئری‌ها، شما می‌توانید Job ها، Exception ها، Event ها، درخواست‌های HTTP و... را نیز به صورت دقیق بررسی کنید.
* **قالب‌بندی زیبا:** کوئری‌ها را به صورت بسیار خوانا (با Syntax Highlighting) نمایش می‌دهد.

**نصب Telescope:**

<div dir="ltr">

``` bash 
    composer require laravel/telescope --dev
    php artisan telescope:install
    php artisan migrate
```
</div>

 **⚠️ هشدار پروداکشن:** Telescope تمام اطلاعات را در دیتابیس ذخیره می‌کند. در محیط پروداکشن با ترافیک بالا، این ابزار می‌تواند به سرعت دیتابیس را پر کرده و سایت را کند کند. اگر مجبور به استفاده در پروداکشن هستید، حتماً از **Pruning** (پاکسازی خودکار) و محدودیت دسترسی (**Gate**) استفاده کنید.

-----

## ۲. ابزارهای محیط واقعی (Production Monitoring) 🚀

در محیط واقعی، شما نمی‌توانید از Debugbar استفاده کنید. شما به ابزارهایی نیاز دارید که با **کمترین سربار**، سلامت سیستم را رصد کنند.

### ضربان قلب سرور ❤️ Laravel Pulse

ابزار جدید و رسمی لاراول برای مشاهده وضعیت سلامت سرور در یک نگاه.

* **کاربرد:** مشاهده کندترین کوئری‌ها، وضعیت صف‌ها (Queues)، کندترین روت‌ها و کاربران پرمصرف.
* **مزیت:** برخلاف Telescope که همه چیز را با جزئیات ذخیره می‌کند، Pulse داده‌ها را تجمیع (Aggregate) می‌کند و سربار بسیار کمی دارد.

### نگهبان خطاها 🚨 Sentry

بهترین ابزار برای ردیابی خطاها (Error Tracking).

* **کاربرد:** وقتی کاربری با خطای 500 مواجه می‌شود، Sentry تمام جزئیات (خط کد، متغیرها، مرورگر کاربر) را ضبط کرده و به شما ایمیل می‌زند.
* **مزیت:** لاگ‌ها را در سرورهای ابری خود ذخیره می‌کند و فشاری به دیتابیس شما نمی‌آورد.

---

### ## استفاده عملی: دیدن کوئری نهایی Eloquent با `()toSql`

گاهی اوقات، قبل از اجرای یک کوئری پیچیده Eloquent، می‌خواهید ببینید که لاراول دقیقاً چه کوئری SQL ای را تولید می‌کند. متد `()toSql` برای این کار عالی است.

<div dir="ltr">

```php
$query = Post::with('user')
    ->where('status', 'published')
    ->orderBy('published_at', 'desc');

// این کوئری را واقعا اجرا نمی‌کند، فقط رشته آن را برمی‌گرداند.
dd($query->toSql());

// خروجی چیزی شبیه به این خواهد بود:
// "select * from `posts` where `status` = ? order by `published_at` desc"
```
</div>

این تکنیک برای دیباگ کردن کوئری‌های پیچیده بدون نیاز به اجرای آن‌ها و پر کردن لاگ‌ها، بسیار مفید است.

با اجرای این کد و باز کردن Debugbar یا Telescope، شما به وضوح می‌توانید کوئری‌های اضافی و تکراری را مشاهده کرده و برای بهینه‌سازی آن‌ها اقدام کنید.

---
[⏩ درس قبلی: ایندکس‌ها و Migration ها](../03-Migrations-And-Indexes/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [📄 مشاهده نمونه کد](examples/DashboardController.php) | [درس بعدی: مقیاس‌پذیری و الگوهای پیشرفته ⏪](../05-Scalability-And-Advanced-Patterns/README.md)

</div>
