<div dir="rtl">

# بخش ۶، درس ۵: نقش Object Caching و اتصال وردپرس به Redis

تا اینجا تمام تلاش ما بر این بوده که کوئری‌های بهینه‌تری به دیتابیس ارسال کنیم. اما چه می‌شود اگر بتوانیم کاری کنیم که آن کوئری‌ها **اصلاً اجرا نشوند**؟ این دقیقاً کاری است که Object Caching انجام می‌دهد.

### ## سیستم Object Cache در وردپرس
وردپرس یک سیستم کش داخلی به نام **WP_Object_Cache** دارد. این سیستم برای ذخیره کردن نتایج عملیات‌های هزینه‌بر (مانند کوئری‌های دیتابیس) در حافظه طراحی شده تا از اجرای تکراری آن‌ها جلوگیری شود.

**اما یک نکته بسیار مهم وجود دارد:**

**کش پیش‌فرض (Non-persistent):** به صورت پیش‌فرض، این کش **پایدار نیست**. یعنی داده‌های کش‌شده فقط در طول **یک درخواست (Request)** در حافظه باقی می‌مانند. به محض اینکه بارگذاری صفحه تمام شود، کل کش خالی می‌شود. این نوع کش برای جلوگیری از اجرای یک کوئری یکسان چندین بار در یک صفحه مفید است، اما کمکی به درخواست‌های بعدی نمی‌کند.

**کش پایدار (Persistent):** هدف اصلی ما، تبدیل این کش به یک کش پایدار است. این کار با اتصال وردپرس به یک سرور کش خارجی و بسیار سریع مانند **Redis** یا **Memcached** انجام می‌شود. در این حالت، داده‌های کش‌شده بین درخواست‌های مختلف و کاربران مختلف **باقی می‌مانند**.

**آنالوژی:** کش پیش‌فرض وردپرس مانند حافظه کوتاه‌مدت است که با هر پلک زدن پاک می‌شود. یک کش پایدار، مانند حافظه بلندمدت است که اطلاعات را به سرعت به یاد می‌آورد.

### ## نقش Redis و Memcached
سرورهایی مثل Redis و Memcached سرورهای ذخیره‌سازی **درون حافظه‌ای (In-Memory)** هستند. از آنجایی که آن‌ها داده‌ها را در RAM ذخیره می‌کنند، سرعت دسترسی به اطلاعات در آن‌ها هزاران بار سریع‌تر از یک دیتابیس است که روی دیسک کار می‌کند.

با نصب یک پلاگین مخصوص (که به آن Drop-in می‌گویند)، شما به وردپرس می‌گویید که به جای استفاده از حافظه کوتاه‌مدت خود، از Redis یا Memcached به عنوان حافظه بلندمدت استفاده کند.

### ## چه چیزهایی به صورت خودکار کش می‌شوند؟
وقتی یک کش پایدار فعال باشد، وردپرس به صورت هوشمند بسیاری از نتایج را در آن ذخیره می‌کند:
* **تمام گزینه‌های autoload شده** از جدول `wp_options`.
* **نتایج کوئری‌های `WP_Query`**.
* **داده‌های Transients API**.
* اطلاعات کاربران، دسته‌بندی‌ها و...

**نتیجه نهایی:**
فعال‌سازی یک کش پایدار، اغلب **مؤثرترین بهینه‌سازی** است که می‌توانید برای یک سایت وردپرسی انجام دهید. این کار می‌تواند تعداد کوئری‌های دیتابیس را در هر صفحه از ده‌ها یا صدها، به تعداد انگشت‌شماری کاهش دهد و زمان پاسخ‌دهی سرور (TTFB) را به شکل چشمگیری بهبود بخشد.

---
[⏪ درس قبلی: مشکل EAV در wp_postmeta](../04-postmeta-eav-problem/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [📄 مشاهده نمونه کد](examples.php) | [🏁 بازگشت به ابتدای دوره 🏁](../../README.md)

</div>
