<div dir="rtl">

# بخش ۶، درس ۱: آشنایی با ساختار دیتابیس وردپرس

قبل از اینکه بتوانیم مشکلات عملکردی وردپرس را حل کنیم، باید بفهمیم که وردپرس چگونه داده‌های خود را سازماندهی می‌کند. ساختار دیتابیس وردپرس بسیار **انعطاف‌پذیر** طراحی شده تا بتواند انواع مختلف محتوا را در خود جای دهد، اما همین انعطاف‌پذیری، منشأ بسیاری از مشکلات عملکردی آن است.

به طور پیش‌فرض، تمام جداول وردپرس با پیشوند `_wp` شروع می‌شوند. در ادامه با مهم‌ترین جداول آن آشنا می‌شویم.

---

### ## جداول اصلی و کلیدی

در یک نصب استاندارد وردپرس، ۱۲ جدول وجود دارد. ما روی مهم‌ترین آن‌ها تمرکز می‌کنیم:

#### آ) `wp_posts`
این جدول، قلب تپنده وردپرس است. برخلاف نامش، این جدول فقط پست‌های وبلاگ را ذخیره نمی‌کند، بلکه تقریباً **تمام انواع محتوا** در اینجا قرار دارند:
* **پست‌ها (Posts):** مقالات وبلاگ.
* **صفحات (Pages):** صفحات استاتیک مانند "درباره ما".
* **منوها (Navigation Menus):** هر آیتم در منو یک ردیف در این جدول است.
* **رسانه‌ها (Attachments):** هر تصویر یا فایلی که آپلود می‌کنید.
* **انواع پست سفارشی (Custom Post Types):** مثلاً `products` در ووکامرس.

یک ستون کلیدی در این جدول `post_type` است که نوع محتوای هر ردیف را مشخص می‌کند.

---

#### ب) `wp_postmeta`
این جدول به `wp_posts` اجازه می‌دهد که بی‌نهایت انعطاف‌پذیر باشد. هر اطلاعات اضافی یا "فراداده" (Metadata) مربوط به یک پست، در این جدول ذخیره می‌شود. برای مثال:
* قیمت یک محصول در ووکامرس.
* تصویر شاخص یک پست.
* فیلدهای سفارشی (Custom Fields) که توسط پلاگین‌ها اضافه می‌شوند.

این جدول از یک مدل به نام **EAV (Entity-Attribute-Value)** استفاده می‌کند که در درس‌های بعدی مشکلات عملکردی آن را بررسی خواهیم کرد.

---

#### پ) `wp_users` و `wp_usermeta`
* جدول **`wp_users`:** اطلاعات اصلی کاربران مانند نام کاربری، پسورد هش‌شده و ایمیل را در خود نگه می‌دارد.
* جدول **`wp_usermeta`:** مانند `wp_postmeta`، این جدول نیز فراداده‌های مربوط به کاربران را ذخیره می‌کند. مثلاً نام و نام خانوادگی، بیوگرافی یا تنظیمات پروفایل که توسط پلاگین‌ها اضافه می‌شود.

---

#### ت) `wp_options`
این جدول تنظیمات کلی سایت و پلاگین‌ها را ذخیره می‌کند. هر چیزی از عنوان سایت گرفته تا تنظیمات یک پلاگین کش در اینجا قرار دارد. این جدول یک ستون کلیدی به نام `autoload` دارد که یکی از **شایع‌ترین دلایل کندی سایت‌های وردپرسی** است و در درس بعدی به طور کامل به آن می‌پردازیم.

---

#### ث) `wp_terms`, `wp_term_taxonomy`, `wp_term_relationships` (جداول طبقه‌بندی)
این سه جدول با هم سیستم طبقه‌بندی (Taxonomy) وردپرس را تشکیل می‌دهند.
* جدول **`wp_terms`:** نام دسته‌بندی‌ها (Categories)، تگ‌ها (Tags) و هر نوع طبقه‌بندی دیگر در اینجا ذخیره می‌شود (مثلاً 'اخبار' یا 'آموزش').
* جدول **`wp_term_taxonomy`:** مشخص می‌کند که هر `term` یک دسته‌بندی است یا یک تگ.
* جدول **`wp_term_relationships`:** این جدول واسط، پست‌ها را به دسته‌بندی‌ها و تگ‌هایشان متصل می‌کند (رابطه چندبه‌چند بین `wp_posts` و `wp_terms`).

---

#### ج) `wp_comments` و `wp_commentmeta`
این جداول به ترتیب نظرات ثبت‌شده برای پست‌ها و فراداده‌های مربوط به آن نظرات را در خود ذخیره می‌کنند.

### خلاصه و نگاه کلی
ساختار دیتابیس وردپرس بر پایه چند جدول اصلی و جداول `meta` برای انعطاف‌پذیری ساخته شده است. این طراحی به پلاگین‌ها اجازه می‌دهد تا به راحتی داده‌های خود را بدون تغییر ساختار اصلی دیتابیس ذخیره کنند، اما همین موضوع، اگر به درستی مدیریت نشود، منجر به کوئری‌های سنگین و ناکارآمد خواهد شد. در درس‌های بعدی، یاد می‌گیریم چگونه این مشکلات را شناسایی و حل کنیم.

---
[⏩ فصل قبلی: یکپارچه‌سازی با لاراول](../../05-Laravel-Integration/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [درس بعدی: مشکل Autoload در wp_options ⏪](../02-wp-options-autoload/README.md)

</div>
