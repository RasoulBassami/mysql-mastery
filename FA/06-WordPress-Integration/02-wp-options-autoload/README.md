<div dir="rtl">

# بخش ۶، درس ۲: حل معضل عملکردی `wp_options` و `autoload`

جدول `wp_options` محلی برای ذخیره تنظیمات کلی سایت، ویجت‌ها، و تنظیمات پلاگین‌ها و قالب‌هاست. در این جدول یک ستون به ظاهر بی‌خطر به نام `autoload` وجود دارد که می‌تواند سایت شما را به زانو درآورد.

### ## ستون `autoload` چیست و چطور می تواند مشکل ساز باشد؟

ستون `autoload` می‌تواند دو مقدار داشته باشد: `yes` یا `no`. اگر مقدار آن `yes` باشد، وردپرس آن گزینه (option) را در **هر بار بارگذاری صفحه**، چه در بخش کاربری سایت و چه در پیشخوان، به صورت خودکار از دیتابیس خوانده و در حافظه بارگذاری می‌کند.

* **آنالوژی:** تصور کنید برای یک پیاده‌روی کوتاه از خانه خارج می‌شوید، اما به جای برداشتن کلید و کیف پول، کل اثاثیه منزلتان را در یک کوله‌پشتی غول‌پیکر با خود حمل می‌کنید! این دقیقاً کاری است که وردپرس با گزینه‌های `autoload` شده انجام می‌دهد.

این ویژگی برای گزینه‌های پراستفاده (مانند آدرس سایت) خوب است. اما مشکل زمانی شروع می‌شود که پلاگین‌ها و قالب‌ها، داده‌های حجیم یا داده‌هایی که به ندرت استفاده می‌شوند را با `'autoload = 'yes` در این جدول ذخیره می‌کنند. بدتر از آن، بسیاری از پلاگین‌ها پس از حذف شدن، گزینه‌های خود را از این جدول پاک نمی‌کنند.

با گذشت زمان، حجم داده‌ای که در هر درخواست بارگذاری می‌شود آنقدر زیاد می‌شود که باعث کندی شدید سایت و افزایش مصرف حافظه (RAM) سرور می‌شود.

---

### ## چگونه مشکل را تشخیص دهیم؟

اولین قدم این است که بفهمیم چه مقدار داده به صورت خودکار بارگذاری می‌شود. این کار را می‌توان با یک کوئری ساده SQL انجام داد.

<div dir="ltr">

```sql
SELECT SUM(LENGTH(option_value)) / 1024 / 1024 AS autoload_size_mb
FROM wp_options
WHERE autoload = 'yes';
```
</div>

این کوئری حجم کل داده‌های `autoload` شده را بر حسب مگابایت به شما نشان می‌دهد.

* **قانون کلی:**
    * **زیر ۱ مگابایت:** عالی.
    * **بین ۱ تا ۳ مگابایت:** قابل قبول است، اما جای بهینه‌سازی دارد.
    * **بالای ۳-۵ مگابایت:** شما یک مشکل عملکردی جدی دارید.
    * **بالای ۱۰ مگابایت:** وضعیت بحرانی است!

---

### ## چگونه مشکل را حل کنیم؟

حل مشکل یک فرآیند دو مرحله‌ای است: ابتدا گزینه‌های حجیم را پیدا می‌کنیم و سپس آن‌ها را بهینه می‌کنیم.

#### مرحله ۱: پیدا کردن گزینه‌های حجیم

با کوئری زیر، می‌توانید ۲۰ گزینه حجیمی که به صورت خودکار بارگذاری می‌شوند را پیدا کنید:
<div dir="ltr">

```sql
SELECT
    option_name,
    LENGTH(option_value) AS option_size_bytes
FROM
    wp_options
WHERE
    autoload = 'yes'
ORDER BY
    option_size_bytes DESC
LIMIT 20;
```
</div>

با دیدن نام گزینه‌ها (`option_name`)، معمولاً می‌توانید حدس بزنید که مربوط به کدام پلاگین یا قالب هستند.

#### مرحله ۲: بهینه‌سازی (تغییر `autoload` به `no`)

برای گزینه‌هایی که شناسایی کرده‌اید (به خصوص آن‌هایی که مربوط به پلاگین‌های غیرفعال یا حذف شده هستند، یا می‌دانید که در هر صفحه نیازی به آن‌ها نیست)، باید مقدار `autoload` را از `yes` به `no` تغییر دهید.
<div dir="ltr">

```sql
UPDATE wp_options SET autoload = 'no' WHERE option_name = 'some_bloated_plugin_option';
```
</div>

**هشدار بسیار مهم: ☠️**
> قبل از اجرای هرگونه دستور `UPDATE` یا `DELETE` روی این جدول، **حتماً یک بک‌آپ کامل از دیتابیس خود بگیرید.** تغییر `autoload` برای یک گزینه حیاتی می‌تواند سایت شما را از کار بیندازد. این کار را با احتیاط کامل انجام دهید.

با تغییر این مقدار به `no`، آن گزینه از بارگذاری خودکار حذف می‌شود، اما داده‌ی آن همچنان در دیتابیس وجود دارد و پلاگین مربوطه در صورت نیاز می‌تواند آن را با تابع `()get_option` مستقیماً بخواند.

---
[⏩ درس قبلی: ساختار دیتابیس وردپرس](../01-Database-Structure/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [📄 مشاهده نمونه کد](examples.sql) | [درس بعدی: کلاس wpdb$ ⏪](../03-wpdb-class/README.md)

</div>
