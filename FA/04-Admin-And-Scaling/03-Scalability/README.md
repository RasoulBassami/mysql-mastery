<div dir="rtl">

# بخش ۴، درس ۳: مقیاس‌پذیری (Scalability)

وقتی اپلیکیشن شما رشد می‌کند، بار روی دیتابیس به شدت افزایش می‌یابد. یک سرور، هرچقدر هم که قوی باشد، در نهایت به محدودیت‌های CPU، RAM و دیسک خود می‌رسد. مقیاس‌پذیری، استراتژی‌هایی برای مدیریت این رشد و توزیع بار بین چندین سرور است.

دو مسیر اصلی برای مقیاس‌پذیری وجود دارد:

* **مقیاس‌پذیری عمودی (Vertical Scaling):** خرید یک سرور قوی‌تر. این ساده‌ترین راه است: RAM بیشتر، CPU سریع‌تر، دیسک‌های NVMe. اما این روش **گران** است و یک **سقف مشخص** دارد. شما نمی‌توانید تا ابد یک سرور قوی‌تر بخرید.
* **مقیاس‌پذیری افقی (Horizontal Scaling):** توزیع بار بین چندین سرور ارزان‌تر. این روش پیچیده‌تر است اما تقریباً **بدون محدودیت** قابلیت رشد دارد. تمرکز ما در این درس بر روی این روش است.

دو تکنیک اصلی برای مقیاس‌پذیری افقی عبارتند از: **تکثیر (Replication)** و **خرد کردن (Sharding)**.

---

## ۱. تکثیر (Replication) برای مقیاس‌پذیری خواندن (Read)

فرآیند **Replication** فرآیند ایجاد و نگهداری کپی‌های یکسان از دیتابیس شما بر روی چندین سرور است. رایج‌ترین معماری برای این کار **Master-Slave Replication** نام دارد.

### ساختار Master-Slave Replication چگونه کار می‌کند؟
1.  یک سرور به عنوان **Master (ارباب)** تعیین می‌شود. این سرور **تمام عملیات نوشتن** (`INSERT`, `UPDATE`, `DELETE`) را مدیریت می‌کند.
2.  هر تغییری که در Master رخ می‌دهد، در یک فایل لاگ مخصوص به نام **Binary Log (binlog)** ثبت می‌شود.
3.  یک یا چند سرور دیگر به عنوان **Slave (برده)** تنظیم می‌شوند. Slave ها به Master متصل شده، binlog را می‌خوانند و تمام تغییرات را روی کپی محلی خودشان **تکرار می‌کنند**.

نتیجه این است که Slave ها کپی‌هایی تقریباً لحظه‌ای (near real-time) از Master هستند.



### کاربرد اصلی: Read/Write Splitting
اینجا جادوی Replication برای مقیاس‌پذیری مشخص می‌شود. از آنجایی که اکثر اپلیکیشن‌ها (مانند یک وبلاگ یا فروشگاه) **تعداد خواندن (Read) بسیار بیشتری** نسبت به نوشتن (Write) دارند، ما می‌توانیم:
* اپلیکیشن را طوری تنظیم کنیم که **تمام کوئری‌های نوشتن** را به سرور **Master** ارسال کند.
* و **تمام کوئری‌های خواندن (`SELECT`)** را بین چندین سرور **Slave** توزیع کند.

**مزیت بزرگ:** با این کار، بار سنگین کوئری‌های خواندن از روی دوش Master برداشته می‌شود. اگر سرعت خواندن کم شد، کافی است یک Slave جدید به مجموعه اضافه کنید!

**سایر مزایا:**
* **در دسترس‌پذیری بالا (High Availability):** اگر سرور Master از کار بیفتد، می‌توان یکی از Slave ها را به عنوان Master جدید معرفی کرد (فرآیندی به نام Failover).
* **پشتیبان‌گیری بدون فشار:** می‌توان عملیات سنگین پشتیبان‌گیری را روی یکی از Slave ها انجام داد بدون اینکه عملکرد سرور اصلی مختل شود.

### ⚠️ چالش بزرگ: تاخیر در تکثیر (Replication Lag)

ارتباط بین Master و Slave معمولاً **آسنکرون (Asynchronous)** است. یعنی Master صبر نمی‌کند تا داده‌ها در Slave کپی شوند و بعد پاسخ موفقیت بدهد. این باعث ایجاد فاصله‌ی زمانی کوتاهی (چند میلی‌ثانیه تا چند ثانیه) می‌شود که داده‌ها در Slave هنوز قدیمی هستند.

**سناریوی خطرناک (Stale Read):**
۱. کاربر پروفایل خود را ویرایش می‌کند (Update در Master).
۲. بلافاصله صفحه را رفرش می‌کند تا تغییرات را ببیند (Select از Slave).
۳. چون Slave هنوز آپدیت نشده، کاربر اطلاعات قدیمی را می‌بیند\!

**راه‌حل معماری (Sticky Sessions):**
راهکار استاندارد این است که اپلیکیشن شما هوشمند باشد: اگر کاربری در یک درخواست (Request) عملیات نوشتن انجام داد، تا پایان آن درخواست، تمام خواندن‌هایش را نیز اجباراً از Master انجام دهد تا داده‌های خودش را ببیند.
*(نحوه پیاده‌سازی این تکنیک در لاراول را در **بخش پنجم** بررسی خواهیم کرد).*

---

## ۲. خرد کردن (Sharding) برای مقیاس‌پذیری نوشتن (Write)

فرآیند **Sharding** فرآیند تقسیم یک دیتابیس بسیار بزرگ به قطعات کوچکتر، سریع‌تر و قابل مدیریت‌تر به نام **Shard** است. هر Shard، خود یک دیتابیس مستقل و کامل است.

### چه زمانی به Sharding نیاز داریم؟
زمانی که حجم **نوشتن** آنقدر زیاد می‌شود که یک سرور Master دیگر توانایی پاسخگویی ندارد، یا زمانی که یک جدول به قدری بزرگ شده (مثلاً میلیاردها ردیف) که مدیریت آن (مثل ایندکس‌گذاری یا پشتیبان‌گیری) غیرممکن می‌شود. Sharding راهکار نهایی برای مقیاس‌پذیری نوشتن است.

* **آنالوژی:** به جای داشتن یک دفترچه تلفن غول‌پیکر برای کل جهان (یک سرور Master)، شما دفترچه‌های تلفن جداگانه‌ای برای هر کشور (Shard) ایجاد می‌کنید. برای پیدا کردن اطلاعات یک شخص، ابتدا باید بدانید در کدام کشور (Shard) زندگی می‌کند.

### چالش‌های Sharding (بخش سخت ماجرا)
* **پیچیدگی در لایه اپلیکیشن:** منطق پیدا کردن Shard مناسب (بر اساس یک **Shard Key** مانند `user_id`) به اپلیکیشن شما منتقل می‌شود.
* **JOIN های بین Shard ها:** اجرای `JOIN` بین جداولی که روی Shard های مختلف قرار دارند، بسیار دشوار و ناکارآمد است.
* **مدیریت و نگهداری:** اضافه کردن یک Shard جدید و توزیع مجدد داده‌ها یک عملیات بسیار پیچیده است.

> **نتیجه‌گیری:** Replication یک استراتژی رایج و بسیار کارآمد برای افزایش ظرفیت خواندن است. Sharding یک تکنیک بسیار قدرتمند برای مقیاس‌های عظیم است، اما به دلیل پیچیدگی بالا، باید به عنوان آخرین راه‌حل در نظر گرفته شود.

---
[⏩ درس قبلی: پشتیبان‌گیری و بازیابی](../02-Backup-And-Recovery/README.md) | [🔼 بازگشت به فهرست فصل](../README.md) | [درس بعدی: عملیات و نگهداری ⏪](../04-Operations/README.md)

</div>
